{
  "name": "@hypergolic/state-lab-napplet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": "24.20.0"
  },
  "napplet": {
    "dTag": "state-lab",
    "title": "State Lab",
    "requires": [
      "identity",
      "storage"
    ]
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && vite build --mode peer",
    "type-check": "tsc --noEmit",
    "test": "playwright test",
    "test:conformance": "napplet-conformance ./dist && napplet-conformance ./dist-peer",
    "verify": "pnpm type-check && pnpm build && pnpm test && pnpm test:conformance",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@napplet/sdk": "0.27.2"
  },
  "devDependencies": {
    "@napplet/conformance-cli": "0.2.18",
    "@napplet/vite-plugin": "0.14.1",
    "@playwright/test": "1.63.0",
    "typescript": "6.0.3",
    "vite": "7.3.6"
  }
}
