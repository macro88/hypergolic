// swift-tools-version: 6.2
import PackageDescription

let package = Package(
  name: "ApprovalTransport",
  platforms: [.macOS(.v14)],
  products: [.library(name: "ApprovalTransport", targets: ["ApprovalTransport"])],
  targets: [
    .target(name: "ApprovalTransport"),
    .testTarget(name: "ApprovalTransportTests", dependencies: ["ApprovalTransport"]),
  ]
)
