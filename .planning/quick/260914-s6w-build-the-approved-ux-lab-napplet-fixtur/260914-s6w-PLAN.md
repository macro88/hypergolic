---
quick_id: 260914-s6w
status: complete
mode: quick
execution: inline
---

# Build UX Lab as the first automated-test napplet

The approved suite is UX Lab, State Lab and Approval Lab, in that order. This task implements the standalone UX Lab fixture and its browser checks. Native Hypergolic gestures, WebView hosting, storage and signing remain subsequent work. The existing mobile scaffold and unrelated local iOS edits are preserved.

1. Build `napplets/ux-lab` as an isolated pnpm package: generated single-file HTML and unsigned NIP-5D manifest sidecar, no hard capability requirements, optional SDK theme, stable semantic/test controls, vertical and horizontal scrolling, input, counter and reset. Keep native/package pins unchanged.
2. Exercise built artifacts inside opaque sandboxed iframes in real browsers: separate instances, hide/show preservation, recreate/reset behavior, content scrolling, optional-theme changes/failure, accessibility and narrow layouts. Run upstream conformance and inspect any version gaps; do not call browser evidence native-host verification.
3. Reconcile stale profile-only planning language, document fixture commands and exact reference/tool versions, record evidence and limitations, and update quick-task state. Stage only task changes, preserving pre-existing edits. Keep private operational context in the Notes vault.

Verification: fixture typecheck/build, browser suite, upstream conformance, artifact/hash audit, applicable repository checks and diff/privacy review. Publication of Nostr artifacts awaits the designated signer and destinations. The local test relay is agreed but is not needed by this capability-free fixture.
