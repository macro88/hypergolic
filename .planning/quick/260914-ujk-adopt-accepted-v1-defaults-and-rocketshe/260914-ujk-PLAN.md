---
quick_id: 260914-ujk
status: complete
mode: quick
execution: inline
---

# Adopt accepted v1 defaults and RocketShell relay configuration

1. Verify RocketShell's current default branch revision and trace production relay defaults through first-run settings and routing. Preserve network and lookup roles, and record immutable source references.
2. Add the selected relay lists as plain startup configuration. Record the accepted six product decisions: approval recovery, workspace recovery, first-open trust, explicit version updates, identity safeguards and editable relays with NIP-65 deferred. Distinguish configuration from an implemented native relay connection/settings UI.
3. Validate the JSON against the inspected source, check secure URL syntax, uniqueness and order, run source typecheck and documentation/link/privacy checks, and commit only task-owned source changes. Keep existing iOS/ignore edits and the Expo pin.
4. Capture decisions and source evidence in the existing shared project notes, update quick-task state/summary, and verify the authorized private-vault sync.

No new NAP wire fields, signer, network client, key store, native dependency or app UI are introduced in this bounded task. Live relay health/authentication and native integration remain separate checks. No source push is included.
