---

phase: "02-independent-android-proof"

plan: "04"

type: "execute"

wave: 4

depends_on: ["02-03"]

files_modified: ["src/security/approval-service.ts", "src/security/event-snapshot.ts", "src/shell/ApprovalSheet.tsx", "src/network/relay-service.ts", "runtime/src/capabilities.ts", "tests/relay/server.mjs", "tests/security/approval.test.ts", "napplets/approval-lab/package.json", "napplets/approval-lab/pnpm-lock.yaml", "napplets/approval-lab/pnpm-workspace.yaml", "napplets/approval-lab/vite.config.ts", "napplets/approval-lab/tsconfig.json", "napplets/approval-lab/index.html", "napplets/approval-lab/src/main.ts", "docs/native-contract.md", "src/security/identity-transition.ts", "src/shell/Overview.tsx", "tests/native/driver.py", "src/network/relay-auth.ts", "tests/network/relay.test.ts", "runtime/tests/capabilities.test.ts", "docs/architecture.md", "runtime/src/main.ts", "src/runtime/NappletHost.tsx", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "runtime/tools/build.mjs", "runtime/fixtures/lock.json", "runtime/tests/runtime.spec.ts"]

autonomous: true

requirements: ["SIGN-01", "SIGN-02", "SIGN-03", "SIGN-04", "SIGN-05", "SIGN-06", "RELAY-02", "ID-04", "SEC-01", "FIX-01", "FIX-03"]

estimate: {"tokens": 47000, "raw_tokens": 47000, "tasks": 3, "confidence": "low"}

must_haves: {"truths": ["Every supported event and relay-auth signature requires exact individual approval from trusted shell.", "Unfocused requests defer; dismissal rejects one and pauses; close/switch/restart and stale callbacks cannot sign or publish.", "Independent relay observations verify canonical approved events and distinguish acceptance, rejection and unknown publication."], "artifacts": [{"path": "src/security/approval-service.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/security/event-snapshot.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/network/relay-service.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/shell/ApprovalSheet.tsx", "provides": "Required executable implementation or direct acceptance evidence"}], "key_links": [{"from": "runtime/src/capabilities.ts", "to": "src/security/approval-service.ts", "via": "all supported signing/outbox requests through native session guard"}, {"from": "src/shell/ApprovalSheet.tsx", "to": "src/security/event-snapshot.ts", "via": "exact immutable reviewed payload"}, {"from": "src/security/approval-service.ts", "to": "src/network/relay-service.ts", "via": "revocable exact-event publication"}]}

---



<objective>
Full individual approval and revocable native capabilities. Implements D-09, D-11, D-12, D-16.

Purpose: advance the complete independent Android proof without replacing it with the first trace.
Output: working slice, direct verification and updated source documentation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-RESEARCH.md
@docs/architecture.md
@docs/development.md
@docs/native-contract.md
</context>

<execution_notes>
Commands run from repository root after enabling pinned tools. New commands/scripts named below are created in their owning task before verification; they do not exist in the scaffold baseline. First failing check is expected until implementation. Native checks need a live explicitly selected target and installed current build; use the tracked driver to observe state and preserve process handles. Short unit checks run after each change; native build/device suites may exceed 60 seconds and must run with retained handles and progress updates. No source push or remote changes. Do not mark pending downstream requirements satisfied by a narrower trace. Some local-module/fixture packaging tasks exceed the five-file guideline because the requested six milestones retain coupled source/manifest/lock/test artifacts; execute as cohesive task commits and split execution internally if context requires, never omit files or behavior.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">

<name>Task 1: Trace Approval Lab request through exact review, signing and local relay</name>

<files>src/security/approval-service.ts, src/security/event-snapshot.ts, src/shell/ApprovalSheet.tsx, src/network/relay-service.ts, runtime/src/capabilities.ts, tests/relay/server.mjs, tests/security/approval.test.ts, napplets/approval-lab/package.json, napplets/approval-lab/pnpm-lock.yaml, napplets/approval-lab/pnpm-workspace.yaml, napplets/approval-lab/vite.config.ts, napplets/approval-lab/tsconfig.json, napplets/approval-lab/index.html, napplets/approval-lab/src/main.ts, docs/native-contract.md, runtime/src/main.ts, src/runtime/NappletHost.tsx, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt, runtime/tools/build.mjs, runtime/fixtures/lock.json, runtime/tests/runtime.spec.ts</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/native-contract.md</read_first>

<behavior>Mutation after enqueue cannot alter approved payload; output signature/content/tags/pubkey/hash independently match; unsupported operation returns owned protocol error.</behavior>

<action>Extend runtime/tools/build.mjs and the byte-verified bundled fixture manifest inventory to include Approval Lab, with a build-time allowlisted fixture map and native-owned selection before the native trace. Each fixture retains independently checked original bytes, hash, aggregate and declared capabilities. Add runtime tests proving only registered fixture identities select their expected bytes; merely building its napplet directory is insufficient. Extend the same existing host main.ts, Expo wrapper and native Module/View with typed identity/signing/relay adapters required by the selected NAP contracts. Specify request/reply direction, method allowlist, schema/size/rate limits, native session ownership, exact approved payload and native-to-host response binding. No arbitrary eval or generic native bridge. Native navigation admission must deny or revoke before a replacement iframe document can reuse the same WindowProxy and session authority; post-load teardown alone is insufficient. Test navigation racing a privileged request at the actual native boundary. Hook Approval Lab through the genuine public capability API, native registry, approval service and validated reply path; test wrong-generation and late responses as well as denial producing no signature or relay event. Per D-11/D-16 pin owning outbox/signing/relay protocol documents, selected operations and errors before coding adapters. Build Approval Lab using real identity/outbox SDK surface, predictable event, delayed request and visible result. Trusted service deep-clones/freezes exact allowed event fields, final timestamp and destinations; validate kind/safe integers/string tags/byte bounds/pubkey. Sheet derives action/content/napplet/publisher/identity and complete expandable event from immutable snapshot. Approve once signs a new copy under the selected identity then independently reparse/hash/verify before sending to resettable local relay using Applesauce. Reject never signs. Add exact desktop verification and native user-input trace; publisher key is never used as run identity.</action>

<verify><automated>node --experimental-strip-types --test tests/security/approval.test.ts && pnpm --dir napplets/approval-lab verify && python3 tests/native/driver.py --serial emulator-5554 approval-trace --output .tools/evidence/02-04-trace</automated></verify>

<done>A real native approval produces precisely the reviewed signed event observed by local relay; rejection produces no EVENT or AUTH.</done>

</task>

<task type="auto" tdd="true">

<name>Task 2: Enforce queue, focus, dismissal and revocation races</name>

<files>src/security/approval-service.ts, src/security/identity-transition.ts, src/shell/ApprovalSheet.tsx, src/shell/Overview.tsx, tests/security/approval.test.ts, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>No automatic next sheet after dismissal; resume is explicit; background request only marks card; closing cancels; switch during key await blocks result; restart starts empty.</behavior>

<action>Per D-09/D-12 use service-owned queue with four pending/session,16 global,10min expiry. One review only, no bulk action. Unfocused requests wait and overview shows pending indicator; focus enables review. Sheet dismissal rejects current, returns to napplet and pauses remainder until explicit Resume approvals. Consume approval once, recheck focus/foreground/session generation/identity epoch/expiry before and after key await and before exposing/publishing. On close, identity transition, renderer loss and process restart invalidate all old callbacks/subscriptions; queue never persists. Test duplicate/replayed requests, expired/current/in-flight cancellation, delayed key read, background request and restart. Permission callbacks share epoch binding.</action>

<verify><automated>node --experimental-strip-types --test tests/security/approval.test.ts && python3 tests/native/driver.py --serial emulator-5554 approval-lifecycle --output .tools/evidence/02-04-lifecycle</automated></verify>

<done>Each request requires separate review; dismissal reliably exits; no stale-session or wrong-identity signature/publication survives lifecycle races.</done>

</task>

<task type="auto" tdd="true">

<name>Task 3: Prove publication failure, auth approval and native capability confinement</name>

<files>src/network/relay-service.ts, src/network/relay-auth.ts, tests/relay/server.mjs, tests/network/relay.test.ts, runtime/tests/capabilities.test.ts, docs/native-contract.md, docs/architecture.md</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/native-contract.md, docs/architecture.md</read_first>

<behavior>Challenge rotation invalidates old approval; socket identity switch closes old auth; rejected and disconnected outcomes differ; removing subscription prevents deferred send.</behavior>

<action>Implement R-07 revocable publication using bounded Applesauce subscriptions instead of uncancellable retrying promises; fixed destinations, same-event retries only when explicit, no automatic restart retry. Independent fault relay supports accepted/rejected/delayed OK, disconnect before/after send, no reply, partial acceptance and rotated AUTH challenge. Report per-relay accepted/rejected/unknown/cancelled-before-send; signing is not publication success. NIP-42 kind22242 goes through same trusted individual approval or returns auth-required, bound socket/relay/challenge/epoch; no direct signer passed into auto-auth. Cross-check full native service allowlist and owned error semantics; no external signer/encryption/INC/media/resource authority slips in from broad package exports. Record native attempts to bypass every privileged route.</action>

<verify><automated>node --experimental-strip-types --test tests/network/relay.test.ts && pnpm --dir runtime test && python3 tests/native/driver.py --serial emulator-5554 approval-faults --output .tools/evidence/02-04-faults</automated></verify>

<done>Server-observed frames prove denied/revoked requests never emit; timeout after dispatch is unknown; every auth signature requires current explicit approval.</done>

</task>

</tasks>

<verification>
Run every task check against current source. Record app/fixture hashes and direct native evidence separately from browser and unit results. Inspect git diff --check, status and remotes. Update must-have evidence; any missing dependent external input remains open.
</verification>

<success_criteria>
All plan truths are proven at their stated scope; task checks pass or an existing explicitly documented toolchain mismatch is identified without masking new failures. No new silent-signing/native authority or unrelated scope. Full phase completion remains subject to final requirement-by-requirement audit in 02-06.
</success_criteria>

<output>
Create .planning/phases/02-independent-android-proof/02-04-SUMMARY.md, recording changes, exact checks, current artifact/device evidence and unresolved rows.
</output>
