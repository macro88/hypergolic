---

phase: "02-independent-android-proof"

plan: "05"

type: "execute"

wave: 6

depends_on: ["02-04"]

files_modified: ["src/napplets/resolve-link.ts", "src/napplets/verified-artifact.ts", "src/napplets/loader.ts", "src/shell/OpenNapplet.tsx", "src/shell/TrustPrompt.tsx", "tests/napplets/loading.test.ts", "docs/native-contract.md", "src/napplets/updates.ts", "src/security/scoped-storage.ts", "src/shell/UpdatePrompt.tsx", "src/security/shell-database.ts", "tests/native/driver.py", "src/network/relay-settings.ts", "src/network/profile.ts", "src/shell/RelaySettings.tsx", "src/shell/Header.tsx", "src/config/default-relays.json", "tests/network/relay-settings.test.ts", "docs/relays.md", "runtime/src/main.ts", "src/runtime/NappletHost.tsx", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "tests/native/ios-driver.py", "modules/napplet-host/ios/NappletHostModule.swift", "modules/napplet-host/ios/NappletHostView.swift"]

autonomous: true

requirements: ["LOAD-01", "LOAD-02", "LOAD-03", "RELAY-01", "RELAY-02", "UX-09", "STATE-01", "SEC-02", "UX-02"]

estimate: {"tokens": 41000, "raw_tokens": 41000, "tasks": 3, "confidence": "low"}

must_haves: {"truths": ["User opens a compatible published napplet only after manifest/blob verification and first-open publisher/access confirmation.", "Selected code remains pinned until a verified update is accepted, preserving scoped data and protecting unsaved work.", "Network and lookup relays use selected editable persisted defaults and clear service failures."], "artifacts": [{"path": "src/napplets/loader.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/napplets/verified-artifact.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/napplets/updates.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/network/relay-settings.ts", "provides": "Required executable implementation or direct acceptance evidence"}], "key_links": [{"from": "src/shell/OpenNapplet.tsx", "to": "src/napplets/loader.ts", "via": "trusted link opening action"}, {"from": "src/napplets/loader.ts", "to": "src/napplets/verified-artifact.ts", "via": "manifest/signature/hash before execution"}, {"from": "src/napplets/updates.ts", "to": "src/security/scoped-storage.ts", "via": "pinned-version swap retains stable verified namespace"}]}

---



<objective>
Verified published loading, explicit updates and editable relays. Implements D-07, D-09, D-13, D-14, D-15.

Purpose: advance the complete independent Android and iOS proof without replacing it with the first trace.
Output: working slice, direct verification and updated source documentation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-RESEARCH.md
@docs/architecture.md
@docs/development.md
@docs/native-contract.md
@docs/capability-contract.md
</context>

<execution_notes>
User correction D-18/D-19: this shared milestone must run and be validated on Android AND iOS. Use tests/native/driver.py for Android and tests/native/ios-driver.py from 02-07 for corresponding iOS scenarios. Native host changes must update both Kotlin and Swift adapters, including source/origin/main-frame/generation ownership, revocation and focus. Actual iOS screenshots/input/restart/security observations are required; do not close a shared row using Android-only results. Root quality targets are React Doctor100/100 and aislop100/100, with no new suppressions. Public App Store publication is separate from a locally installable standalone iOS build.

Commands run from repository root after enabling pinned tools. New commands/scripts named below are created in their owning task before verification; they do not exist in the scaffold baseline. First failing check is expected until implementation. Native checks need a live explicitly selected target and installed current build; use the tracked driver to observe state and preserve process handles. Short unit checks run after each change; native build/device suites may exceed 60 seconds and must run with retained handles and progress updates. No source push or remote changes. Do not mark pending downstream requirements satisfied by a narrower trace. Some local-module/fixture packaging tasks exceed the five-file guideline because the accepted milestones retain coupled source/manifest/lock/test artifacts; execute as cohesive task commits and split execution internally if context requires, never omit files or behavior.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">

<name>Task 1: Trace a signed published coordinate through verified execution and first-open consent</name>

<files>src/napplets/resolve-link.ts, src/napplets/verified-artifact.ts, src/napplets/loader.ts, src/shell/OpenNapplet.tsx, src/shell/TrustPrompt.tsx, tests/napplets/loading.test.ts, docs/native-contract.md, runtime/src/main.ts, src/runtime/NappletHost.tsx, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt, tests/native/ios-driver.py, modules/napplet-host/ios/NappletHostModule.swift, modules/napplet-host/ios/NappletHostView.swift</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/native-contract.md</read_first>

<behavior>Wrong-kind35128 is not confused with35129; malformed identifier/HTML and oversized blob fail; existing app remains usable after failure.</behavior>

<action>Replace the bundled-only artifact selection path with a native-owned verified-artifact handle resolved from the trusted loader. Update runtime main.ts, the Expo wrapper and native Module/View to deliver only the selected verified descriptor and bounded immutable artifact bytes from the trusted cache; the napplet cannot supply an arbitrary URL, file path or HTML prop. Bind publisher/stable-app/version/access/generation before execution, retain strict outer/child CSP and source checks, and extend the allowlisted asset route only as needed for the documented delivery contract. Prove embedded and published routes converge on the same native execution boundary and verification failure prevents creation of executable content. Per D-13 select supported nostr:naddr/naddr named-kind35129 link coordinate from pinned NIP-19/5D; document exact accepted encoding, pubkey/d identifier/relay hints, allowed network schemes and resolution rules. Add nevent only if same verified contract is explicitly covered; do not silently accept ordinary web URLs as apps. Resolve manifests over lookup relays, validate signature/event kind/coordinate/publisher/aggregate and required blobs/content hashes before executing. Trusted loader registers immutable verified descriptor in native session registry; RN or napplet claims alone cannot authorize arbitrary HTML. Show first-open publisher/access and cancel before execution; allow any compatible publisher after accepted consent. Local controlled signed manifests allow implementation without project publisher credentials. Host parser/CSP injection generalizes through adversarial tests before remote loading enabled.</action>

<verify><automated>node --experimental-strip-types --test tests/napplets/loading.test.ts && python3 tests/native/driver.py --serial emulator-5554 published-local --output .tools/evidence/02-05-load && python3 tests/native/ios-driver.py --udid "$HYPERGOLIC_IOS_UDID" published-local --output .tools/evidence/02-05-load-ios</automated></verify>

<done>A controlled valid signed coordinate opens through the actual settings/native verification path; invalid signature/hash/wrong identity or denied trust prevents executable marker.</done>

</task>

<task type="auto" tdd="true">

<name>Task 2: Implement explicit verified version changes and verified recovery</name>

<files>src/napplets/updates.ts, src/napplets/loader.ts, src/security/scoped-storage.ts, src/shell/UpdatePrompt.tsx, src/security/shell-database.ts, tests/napplets/loading.test.ts, tests/native/driver.py, tests/native/ios-driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Version announcement alone changes nothing; dirty cancel preserves input; accepting invalid update impossible; pending old-version requests revoked; cross-publisher update cannot inherit storage.</behavior>

<action>Per D-07/D-09/D-13 persist selected immutable event/version descriptor, workspace order/last-active/explicit-empty and first-open grants. Discover candidate versions without replacing current. Show verified update explicitly, compare requested access and obtain consent for expansion, apply existing unknown/unsaved warning, then cancel old requests and replace session only after full verified artifact available. Cancel/failure keeps prior pinned session. Same publisher/stable app namespace retains saved state; do not silently migrate unrelated identities/apps. Ordinary restart revalidates cached bytes/event or fetches exact pinned version; unavailable/mismatched artifacts show recovery state and never execute another revision. Test malformed earlier-CSP tokens, redirects, wrong hash and deterministic failed update rollbacks.</action>

<verify><automated>node --experimental-strip-types --test tests/napplets/loading.test.ts && python3 tests/native/driver.py --serial emulator-5554 updates-recovery --output .tools/evidence/02-05-update && python3 tests/native/ios-driver.py --udid "$HYPERGOLIC_IOS_UDID" updates-recovery --output .tools/evidence/02-05-update-ios</automated></verify>

<done>Verified accepted update changes code deliberately while retaining correct saved data; rejection/offline/tamper keeps pinned safety and restart/empty semantics.</done>

</task>

<task type="auto" tdd="true">

<name>Task 3: Wire editable relay roles and profile identity display</name>

<files>src/network/relay-settings.ts, src/network/profile.ts, src/shell/RelaySettings.tsx, src/shell/Header.tsx, src/config/default-relays.json, tests/network/relay-settings.test.ts, docs/relays.md, tests/native/ios-driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/relays.md</read_first>

<behavior>Second launch never overwrites custom lists; invalid URL rejected; profile signature/wrong pubkey ignored; missing/broken photo falls back; identity switch removes old image.</behavior>

<action>Per D-03/D-14, seed existing exact network/lookup arrays once, normalize/validate editable wss URLs, keep roles distinct and preserve edits across restart. Provide add/remove and explicit restore-default behavior with clear unavailable service and auth-required outcomes; no automatic NIP-65 routing. Restrict loopback ws allowance to a visibly configured test environment for local relay tests, not arbitrary insecure public destinations. Fetch selected-identity profile metadata via verified Nostr events and display permitted picture with stable fallback; clear prior identity image during epoch change and validate public key correspondence. Limit remote image redirects/size and failure; no napplet-provided networking bypass. Validate local controlled profile/photo/missing cases and actual RocketShell endpoint health separately.</action>

<verify><automated>node --experimental-strip-types --test tests/network/relay-settings.test.ts && python3 tests/native/driver.py --serial emulator-5554 relay-settings --output .tools/evidence/02-05-relays && python3 tests/native/ios-driver.py --udid "$HYPERGOLIC_IOS_UDID" relay-settings --output .tools/evidence/02-05-relays-ios</automated></verify>

<done>User edits survive restart, roles route correctly, defaults are exact, auth/service errors clear, and header avatar/npub always correspond to selected identity.</done>

</task>

</tasks>

<verification>
Run every task check against current source. Record app/fixture hashes and direct native evidence separately from browser and unit results. Inspect git diff --check, status and remotes. Update must-have evidence; any missing dependent external input remains open.
</verification>

<success_criteria>
All plan truths are proven at their stated scope; task checks pass or an existing explicitly documented toolchain mismatch is identified without masking new failures. No new silent-signing/native authority or unrelated scope. Full phase completion remains subject to final requirement-by-requirement audit in 02-06.
</success_criteria>

<output>
Create .planning/phases/02-independent-android-proof/02-05-SUMMARY.md, recording changes, exact checks, current artifact/device evidence and unresolved rows.
</output>
