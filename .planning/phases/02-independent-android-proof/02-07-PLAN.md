---
phase: "02-independent-android-proof"
plan: "07"
type: "execute"
wave: 2
depends_on: ["02-01"]
files_modified: ["modules/napplet-host/ios/NappletHostModule.swift", "modules/napplet-host/ios/NappletHostView.swift", "modules/napplet-host/ios/HypergolicNappletHost.podspec", "modules/napplet-host/expo-module.config.json", "modules/napplet-host/.gitignore", "scripts/build-runtime.mjs", "runtime/src/main.ts", "runtime/tests/runtime.spec.ts", "src/runtime/NappletHost.tsx", "package.json", "app.json", "plugins/withIdentityProtection.cjs", "tests/native/ios-driver.py", "tests/native/test_ios_driver.py", "tests/native/README.md", "docs/native-contract.md", "docs/development.md"]
autonomous: true
requirements: ["SEC-01", "SEC-02", "SEC-03", "FIX-01", "BUILD-01", "BUILD-02"]
must_haves:
  truths:
    - "iOS runs the same byte-verified UX Lab through real Kehto in a restricted WKWebView."
    - "Native iOS sender/frame/generation checks, navigation denial and teardown are measured independently."
    - "Shared shell milestones have an actual iOS build/input/evidence path; a placeholder cannot satisfy v1."
  artifacts:
    - path: "modules/napplet-host/ios/NappletHostView.swift"
      provides: "Restricted native iOS view and teardown"
    - path: "tests/native/ios-driver.py"
      provides: "Explicit selected Simulator/device trace with actual observations"
  key_links:
    - from: "src/runtime/NappletHost.tsx"
      to: "modules/napplet-host/ios/NappletHostModule.swift"
      via: "Shared Expo native view name with typed session and focus props"
    - from: "scripts/build-runtime.mjs"
      to: "modules/napplet-host/ios/HypergolicNappletHost.podspec"
      via: "Byte-verified generated assets in a dedicated resource bundle"
---

<objective>
Implement the iOS native host required by the user's D-18 correction. Android was
the first trace, not the v1 platform boundary. Run both platforms through the same
accepted product journey, with distinct native containment and device evidence.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-01-SUMMARY.md
@docs/native-contract.md
@docs/development.md
</context>

<execution_notes>
Keep the existing phase directory for stable references. Source remotes and pushes
remain owner-only. Preserve pre-existing iOS scaffolding and Expo57.0.20. Inspect
installed Expo module code and current owning Apple WebKit APIs before selecting
asset origin, message world, deployment minimum and navigation policy. A custom
scheme, WKContentWorld or file URL is not automatically a secure isolated boundary.
Prove WebCrypto availability and document the exact trust path. All later shared
plans require Android and iOS runs. Final React Doctor and aislop scores must both
be100/100; no findings hidden or test failures converted to success. Never claim
Simulator signing/auth evidence as physical-iPhone proof.
</execution_notes>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Real Kehto and UX Lab through the iOS native host</name>
<files>modules/napplet-host/ios/NappletHostModule.swift, modules/napplet-host/ios/NappletHostView.swift, modules/napplet-host/ios/HypergolicNappletHost.podspec, modules/napplet-host/expo-module.config.json, modules/napplet-host/.gitignore, scripts/build-runtime.mjs, runtime/src/main.ts, src/runtime/NappletHost.tsx, package.json, app.json, tests/native/ios-driver.py</files>
<read_first>AGENTS.md, docs/development.md, docs/native-contract.md, runtime/src/main.ts, src/runtime/NappletHost.tsx</read_first>
<behavior>Without the iOS host the trace fails. With it, actual native Ready and fixture Host colors, counter input and typed mirror are observed on an explicitly selected iPhone Simulator.</behavior>
<action>Package exactly the verified trusted runtime assets into a dedicated native resource bundle. Use one fresh native generation and restricted WKWebView per shell descriptor. Choose and verify a bounded trusted URL/origin and secure WebCrypto path; inject no arbitrary HTML/URL authority through React props. Adapt the platform-neutral host diagnostic transport to the real iOS message handler, checking native main-frame identity, exact URL/security origin, view, generation, type/schema and UTF8 size. Use nonpersistent website data and supported public APIs only. Expose the same typed native view and focus setter as Android; unfocusing dismisses HTML input/IME without reload. Build with existing Xcode/CocoaPods tools, preserve Android project/artifacts. The tracked driver must observe the actual app/device, use real input and preserve image/hierarchy/log metadata; unavailable instrumentation fails honestly.</action>
<verify><automated>pnpm run typecheck && pnpm run prebuild:ios && python3 tests/native/ios-driver.py --udid "$HYPERGOLIC_IOS_UDID" trace-host --output .tools/evidence/02-07-trace</automated></verify>
<done>Actual iOS native readiness/theme/input works using the same original fixture hash. Build, Simulator, OS and artifact identity are recorded. No iOS placeholder or desktop browser substitutes.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Prove the iOS trust and lifecycle boundary</name>
<files>modules/napplet-host/ios/NappletHostView.swift, runtime/src/main.ts, runtime/tests/runtime.spec.ts, tests/native/ios-driver.py, tests/native/test_ios_driver.py, tests/native/README.md, docs/native-contract.md</files>
<read_first>docs/native-contract.md, tests/native/README.md, modules/napplet-host/ios/NappletHostView.swift</read_first>
<behavior>Unsupported and wrong-source requests cannot reach authority; genuine theme still works; navigation/renderer loss removes the executable session and ignores late callbacks.</behavior>
<action>Enforce actual WK navigation/action/response policy, no arbitrary file read/network, chooser/download/popup/media/dialog denial and no untrusted direct native handler authority. Record public API availability and choose an explicit safe minimum iOS version if older APIs cannot enforce required denial; never use private APIs or fragile DOM suppression. Use strict child CSP and real Kehto Window binding in addition to native checks. Revoke before replacement-document admission where authority could otherwise follow a WindowProxy. Test main-frame/subframe/trusted world distinctions, invalid/oversized diagnostics, original fixture integrity, direct parent/native object/storage/network attempts and independent receiver controls. Teardown unregisters handlers, cancels navigation/callback ownership and removes the failed WebView. Label trusted fault injection separately from untrusted attacks; record unsupported exact cases as open final gates.</action>
<verify><automated>pnpm run runtime:test && python3 -m unittest discover -s tests/native -p 'test_*.py' && python3 tests/native/ios-driver.py --udid "$HYPERGOLIC_IOS_UDID" host-boundary --output .tools/evidence/02-07-boundary</automated></verify>
<done>Selected iOS boundary cases have direct native evidence, with exact limitations and no inferred Android-to-iOS passes. Privileged operations remain disabled until their own adapters are validated.</done>
</task>

<task type="auto">
<name>Task 3: Reproducible two-platform build and shared milestone handoff</name>
<files>package.json, scripts/build-runtime.mjs, tests/native/README.md, docs/development.md, docs/native-contract.md, .planning/STATE.md</files>
<read_first>package.json, docs/development.md, .planning/REQUIREMENTS.md</read_first>
<action>Add real iOS runtime build/prebuild/export commands alongside Android, with generated asset verification and absent-asset build failure. Verify existing iOS native Debug build and current Android build still work; record both exact artifacts. Prepare local iOS Release build path without Metro, keeping account enrollment and owner signing custody separate. The final physical-iPhone signing/authentication/device journey is owned by02-06 and must remain open until supplied. Document tested deployment minimum and full remaining capability scope. Update shared plans02-02 through02-06 so native input, identity, storage, approval, loading and final build evidence run on both platforms. Commit task changes locally only, retaining existing unrelated iOS edits.</action>
<verify><automated>pnpm run typecheck && pnpm run doctor:react && pnpm run doctor:slop && pnpm run export:android && pnpm run export:ios && git diff --check b8b291e</automated></verify>
<done>Both native build routes and actual first traces are documented. Shared UI work can run on both; no final product/physical-device claim is fabricated.</done>
</task>
</tasks>

<verification>
Keep Android and iOS results separate. Require real native code, current hashes,
screenshots/input/lifecycle observations and explicit device selection. Check all
task-owned diffs, quality scores and preserved source remotes. External device or
signing inputs do not block independent local implementation but cannot be invented.
</verification>
<success_criteria>
Real iOS napplet execution and the tested containment boundary exist alongside the
Android trace. The full v1 requirement remains both-platform acceptance through02-06.
</success_criteria>
<output>
Create02-07-SUMMARY.md with actual checks, limitations and remaining requirements.
</output>
