---

phase: "02-independent-android-proof"

plan: "02"

type: "execute"

wave: 2

depends_on: ["02-01"]

files_modified: ["src/shell/workspace.ts", "src/shell/Shell.tsx", "src/shell/Header.tsx", "src/App.tsx", "src/shell/workspace.test.ts", "tests/native/driver.py", "src/shell/Handles.tsx", "src/shell/Overview.tsx", "src/shell/motion.ts", "src/shell/ClosePrompt.tsx", "docs/architecture.md"]

autonomous: true

requirements: ["UX-01", "UX-03", "UX-04", "UX-05", "UX-06", "UX-07", "UX-08", "UX-09", "UX-10"]

estimate: {"tokens": 38000, "raw_tokens": 38000, "tasks": 3, "confidence": "low"}

must_haves: {"truths": ["Three live UX Lab instances keep distinct input and scroll through handle switching and overview.", "Inset drag follows touch, cancels early, resists at ends and zooms between focus and two-column overview.", "Unknown/dirty close warns, stays in overview after closure, and final closure presents a quiet Open napplet state."], "artifacts": [{"path": "src/shell/workspace.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/shell/Shell.tsx", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/shell/Handles.tsx", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/shell/Overview.tsx", "provides": "Required executable implementation or direct acceptance evidence"}], "key_links": [{"from": "src/shell/Shell.tsx", "to": "src/runtime/NappletHost.tsx", "via": "stable mounted view per workspace session"}, {"from": "src/shell/Handles.tsx", "to": "src/shell/workspace.ts", "via": "bounded previous/next state transition"}, {"from": "src/shell/Overview.tsx", "to": "src/shell/ClosePrompt.tsx", "via": "close request and unsaved-state guard"}]}

---



<objective>
Multi-napplet gestures, overview and live state. Implements D-03, D-04, D-05, D-06, D-07, D-17.

Purpose: advance the complete independent Android proof without replacing it with the first trace.
Output: working slice, direct verification and updated source documentation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-RESEARCH.md
@docs/architecture.md
@docs/development.md
@docs/native-contract.md
</context>

<execution_notes>
Commands run from repository root after enabling pinned tools. New commands/scripts named below are created in their owning task before verification; they do not exist in the scaffold baseline. First failing check is expected until implementation. Native checks need a live explicitly selected target and installed current build; use the tracked driver to observe state and preserve process handles. Short unit checks run after each change; native build/device suites may exceed 60 seconds and must run with retained handles and progress updates. No source push or remote changes. Do not mark pending downstream requirements satisfied by a narrower trace. Some local-module/fixture packaging tasks exceed the five-file guideline because the requested six milestones retain coupled source/manifest/lock/test artifacts; execute as cohesive task commits and split execution internally if context requires, never omit files or behavior.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">

<name>Task 1: Trace three independent loaded views through shell switching</name>

<files>src/shell/workspace.ts, src/shell/Shell.tsx, src/shell/Header.tsx, src/App.tsx, src/shell/workspace.test.ts, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Add appends, focus does not reorder, close removes exactly one session; return observes prior counter/input/markers.</behavior>

<action>Per D-03/D-04/D-05 create stable session IDs and opening order; mount one native runtime view per loaded entry without remounting on focus or overview. New entries append, focus only changes focused ID. Use supplied logo32dp noninteractive, focused name and trusted settings entry; identity rendering consumes real snapshot from 02-03 and must never invent an npub. Initial fixture opening is an explicitly labelled bundled test route, not a catalogue. Driver enters distinct values/counters and both-axis scroll in three instances and returns by observed handles. Model tests exercise order/focus/close transitions; native input verifies actual WebView retention.</action>

<verify><automated>node --experimental-strip-types --test src/shell/workspace.test.ts && python3 tests/native/driver.py --serial emulator-5554 switch-state --output .tools/evidence/02-02-state</automated></verify>

<done>Three independently interactive native sessions preserve values/scroll while order remains unchanged.</done>

</task>

<task type="auto" tdd="true">

<name>Task 2: Implement inset gestures and reversible view-to-card zoom</name>

<files>src/shell/Handles.tsx, src/shell/Overview.tsx, src/shell/motion.ts, src/shell/Shell.tsx, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Both end taps open overview, edge controls inside fixture remain usable, drag short release restores focus, sustained inward drag selects adjacent instance.</behavior>

<action>Per D-04/D-05 implement thumb-height inset handles outside OS gesture zones; inward left/right selects previous/next, gesture follows progress, early release cancels and first/last resistance never wraps. Tap either handle at any index opens two-column overview. Animate the actual focused view to its measured card destination, card selection back to focus, without remounting content. Upward card gesture requests closure. Measure card geometry across phone sizes/insets, support reduced motion and cancellation, avoid intercepting fixture horizontal/vertical scroll or keyboard input. Record frames/video for zoom, drag cancellation and resistance; adjust thresholds from observed phone behavior rather than unmeasured constants.</action>

<verify><automated>python3 tests/native/driver.py --serial emulator-5554 gestures --output .tools/evidence/02-02-gestures && pnpm run typecheck</automated></verify>

<done>Native input proves full gesture mappings, two-column zoom geometry, no wrap and no scroll conflict on tested screen; actual phone tuning is recorded in 02-06.</done>

</task>

<task type="auto" tdd="true">

<name>Task 3: Complete close, empty-state and accessible navigation behavior</name>

<files>src/shell/ClosePrompt.tsx, src/shell/Overview.tsx, src/shell/Shell.tsx, src/shell/workspace.test.ts, tests/native/driver.py, docs/architecture.md</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/architecture.md</read_first>

<behavior>Keep open leaves instance values unchanged, Close anyway destroys it, closing last exposes Open napplet, Back never bypasses unsaved confirmation.</behavior>

<action>Per D-06/D-07, clean status closes immediately only through a pinned supported lifecycle contract. Otherwise unknown/dirty requires Keep open default or Close anyway; until an interoperable dirty contract is verified all unreporting napplets correctly remain unknown. Do not fabricate NAP fields or label clean-case acceptance complete without its real contract. Accepted close destroys exactly that view and revokes its requests, keeps saved data, stays in overview and leaves Open napplet via settings after final close. Persistable workspace descriptors retain order/focus/explicit-empty for 02-03 storage; no draft serialization promise. Add Back/keyboard dismissal/focus restoration/accessibility labels and reduced-motion checks. Test deliberate empty versus ordinary restart once 02-03 storage is present; row UX-09 stays open until then.</action>

<verify><automated>node --experimental-strip-types --test src/shell/workspace.test.ts && python3 tests/native/driver.py --serial emulator-5554 closing --output .tools/evidence/02-02-close</automated></verify>

<done>Unknown and dirty protection, Keep open retention, exact teardown, overview persistence and accessible empty/settings flows work. Clean-contract and durable restart status are stated honestly.</done>

</task>

</tasks>

<verification>
Run every task check against current source. Record app/fixture hashes and direct native evidence separately from browser and unit results. Inspect git diff --check, status and remotes. Update must-have evidence; any missing dependent external input remains open.
</verification>

<success_criteria>
All plan truths are proven at their stated scope; task checks pass or an existing explicitly documented toolchain mismatch is identified without masking new failures. No new silent-signing/native authority or unrelated scope. Full phase completion remains subject to final requirement-by-requirement audit in 02-06.
</success_criteria>

<output>
Create .planning/phases/02-independent-android-proof/02-02-SUMMARY.md, recording changes, exact checks, current artifact/device evidence and unresolved rows.
</output>
