---

phase: "02-independent-android-proof"

plan: "01"

type: "execute"

wave: 1

depends_on: []

files_modified: ["runtime/package.json", "runtime/pnpm-lock.yaml", "runtime/pnpm-workspace.yaml", "runtime/tsconfig.json", "runtime/src/main.ts", "runtime/tools/build.mjs", "modules/napplet-host/expo-module.config.json", "modules/napplet-host/package.json", "modules/napplet-host/android/build.gradle", "modules/napplet-host/android/src/main/AndroidManifest.xml", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "src/runtime/NappletHost.tsx", "src/App.tsx", "tests/native/driver.py", "runtime/tests/runtime.spec.ts", "package.json", "scripts/build-runtime.mjs", "docs/native-contract.md", "docs/architecture.md", "docs/development.md"]

autonomous: true

requirements: ["SEC-01", "SEC-02", "SEC-03", "FIX-01", "BUILD-01"]

estimate: {"tokens": 42000, "raw_tokens": 42000, "tasks": 3, "confidence": "low"}

must_haves: {"truths": ["The built UX Lab runs inside the real Kehto host in an Android WebView, and a real theme request returns through the selected contract.", "Only the bound opaque iframe reaches the trusted host; unsupported/native escape traffic is blocked.", "Native trace evidence identifies app/fixture hash, device and WebView; no identity or published verification is fabricated."], "artifacts": [{"path": "runtime/src/main.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/runtime/NappletHost.tsx", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "tests/native/driver.py", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "runtime/tools/build.mjs", "provides": "Builds manifest-bound index.html and host.js assets"}, {"path": "scripts/build-runtime.mjs", "provides": "Verifies and copies generated assets into native module"}], "key_links": [{"from": "runtime/tools/build.mjs", "to": "scripts/build-runtime.mjs", "via": "runtime/dist/index.html and host.js plus assets-manifest.json build/verify/copy"}, {"from": "scripts/build-runtime.mjs", "to": "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "via": "copied android/src/main/assets/runtime assets served only by main-frame WebViewAssetLoader https://appassets.androidplatform.net/assets/runtime/index.html"}, {"from": "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "to": "runtime/src/main.ts", "via": "trusted index.html loads host.js; exact allowed native origin/main-frame/generation listener owns channel"}, {"from": "runtime/src/main.ts", "to": "runtime/fixtures/ux-lab.html", "via": "hash-checked immutable fixture then prelude/CSP injection; Window-bound originRegistry and createShellBridge for opaque iframe"}, {"from": "src/runtime/NappletHost.tsx", "to": "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt", "via": "Expo native view registration delegates to generation-bound NappletHostView"}]}

---



<objective>
Native host and first actual fixture trace. Implements D-01, D-02, D-15, D-17.

Purpose: advance the complete independent Android proof without replacing it with the first trace.
Output: working slice, direct verification and updated source documentation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-RESEARCH.md
@docs/architecture.md
@docs/development.md
</context>

<execution_notes>
Commands run from repository root after enabling pinned tools. New commands/scripts named below are created in their owning task before verification; they do not exist in the scaffold baseline. First failing check is expected until implementation. Native checks need a live explicitly selected target and installed current build; use the tracked driver to observe state and preserve process handles. Short unit checks run after each change; native build/device suites may exceed 60 seconds and must run with retained handles and progress updates. No source push or remote changes. Do not mark pending downstream requirements satisfied by a narrower trace. Some local-module/fixture packaging tasks exceed the five-file guideline because the requested six milestones retain coupled source/manifest/lock/test artifacts; execute as cohesive task commits and split execution internally if context requires, never omit files or behavior.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">

<name>Task 1: Trace UX Lab through bundled Kehto and the restricted native view</name>

<files>runtime/package.json, runtime/pnpm-lock.yaml, runtime/pnpm-workspace.yaml, runtime/tsconfig.json, runtime/src/main.ts, runtime/tools/build.mjs, modules/napplet-host/expo-module.config.json, modules/napplet-host/package.json, modules/napplet-host/android/build.gradle, modules/napplet-host/android/src/main/AndroidManifest.xml, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt, src/runtime/NappletHost.tsx, src/App.tsx, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Before wiring, the driver fails because the native fixture does not exist; afterward it observes Ready, a real counter change and established Kehto session/theme result.</behavior>

<action>Per D-02/D-15, create the isolated web-host build using R-01 pins and generated Android assets index.html and host.js under modules/napplet-host/android/src/main/assets/runtime/. Use one Expo native view with WebViewAssetLoader and exactly one opaque allow-scripts iframe. Verify original built UX HTML hash against bundled fixture manifest before namespace/CSP injection. Wire the actual Kehto createShellBridge, namespace prelude and originRegistry; only shell.ready and theme.get are enabled in this trace. Implement exact-origin/main-frame WebMessageListener feature gating and a private native-issued session generation. No external key or relay dependency. Build a tracked portable ADB driver derived from the inspected existing helper, with explicit serial, observation-based waits and JSON/screenshot/XML evidence. It must fail if the device/app/fixture observation is absent. Start/reuse one emulator and Metro under retained handles; no broad process killing or data wipes. This trace proves the future runtime boundary, not full product acceptance.</action>

<verify><automated>python3 tests/native/driver.py --serial emulator-5554 trace-host --output .tools/evidence/02-01-trace</automated></verify>

<done>The installed native app displays interactive UX Lab; Android screenshot plus hierarchy and real handshake/theme trace are recorded, and absent native bridge support shows an error instead of loading code.</done>

</task>

<task type="auto" tdd="true">

<name>Task 2: Enforce source, navigation, network and teardown invariants</name>

<files>runtime/src/main.ts, runtime/tests/runtime.spec.ts, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Wrong frame and direct native object attempts are rejected; allowed theme still works; duplicate ready has one init; process/navigation loss invalidates callback ownership.</behavior>

<action>Apply R-02/R-03 exactly: bare readiness once, theme IDs bounded, source-window match before Kehto; native exact trusted https origin/isMainFrame/generation/schema/byte limits. Asset loader serves only known main host/assets and returns explicit blocked responses for unknown URLs. Deny file/content, cookies, DOM storage, mixed content, chooser/download/popups/permissions/SSL errors and navigation. Inject restrictive CSP before executable input; reject unsupported document shape. Teardown removes listeners, registry, subscriptions and callbacks on close/nav/renderer loss. Add malicious opaque-frame test artifacts within the same runtime transport, with untrusted content and no new native test privilege. Test readiness duplication/forgery, unsupported messages, sibling source and stale generation; independent receiver observes blocked fetch/WS/image/worker/navigation escapes.</action>

<verify><automated>pnpm --dir runtime test && python3 tests/native/driver.py --serial emulator-5554 host-boundary --output .tools/evidence/02-01-boundary</automated></verify>

<done>Both source-contract tests and native attack trace pass; every blocked attempt is measured at the actual boundary and no callback survives destroyed generation.</done>

</task>

<task type="auto">

<name>Task 3: Make the runtime reproducible and record its actual limits</name>

<files>package.json, scripts/build-runtime.mjs, docs/native-contract.md, docs/architecture.md, docs/development.md</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/native-contract.md, docs/architecture.md, docs/development.md</read_first>

<action>Per D-01/D-17, expose runtime build/test scripts and scripts/build-runtime.mjs that builds runtime/dist/index.html and runtime/dist/host.js, verifies assets-manifest.json, and copies exactly those assets into modules/napplet-host/android/src/main/assets/runtime before Android builds, document exact pins/licenses/protocol fields and current capability allowlist. Run root source typecheck, Android export/prebuild and Gradle after native changes. Preserve existing unrelated source diffs and Expo57.0.20. Record actual trace and hardening evidence plus unimplemented full capability/identity/loading requirements. Commit task-owned changes locally only; never alter source remotes or push. Retain portable commands and describe local device requirements without private paths/serials.</action>

<verify><automated>pnpm run typecheck && pnpm run export:android && pnpm run prebuild:android && CI=true ./android/gradlew -p android assembleDebug --no-daemon -PreactNativeArchitectures=arm64-v8a --console=plain</automated></verify>

<done>A fresh generated Android project builds the same local module/assets; docs distinguish first native theme trace from incomplete identity/signing/published shell.</done>

</task>

</tasks>

<verification>
Run every task check against current source. Record app/fixture hashes and direct native evidence separately from browser and unit results. Inspect git diff --check, status and remotes. Update must-have evidence; any missing dependent external input remains open.
</verification>

<success_criteria>
All plan truths are proven at their stated scope; task checks pass or an existing explicitly documented toolchain mismatch is identified without masking new failures. No new silent-signing/native authority or unrelated scope. Full phase completion remains subject to final requirement-by-requirement audit in 02-06.
</success_criteria>

<output>
Create .planning/phases/02-independent-android-proof/02-01-SUMMARY.md, recording changes, exact checks, current artifact/device evidence and unresolved rows.
</output>
