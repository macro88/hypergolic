---

phase: "02-independent-android-proof"

plan: "03"

type: "execute"

wave: 3

depends_on: ["02-02"]

files_modified: ["package.json", "pnpm-lock.yaml", "index.ts", "src/security/crypto-bootstrap.ts", "src/security/identity-vault.ts", "src/security/shell-database.ts", "src/security/identity-vault.test.ts", "src/shell/Header.tsx", "tests/native/driver.py", "src/security/identity-transition.ts", "src/security/scoped-storage.ts", "src/shell/Settings.tsx", "runtime/src/capabilities.ts", "napplets/state-lab/package.json", "napplets/state-lab/src/main.ts", "napplets/state-lab/vite.config.ts", "napplets/state-lab/tsconfig.json", "napplets/state-lab/index.html", "napplets/state-lab/pnpm-lock.yaml", "napplets/state-lab/pnpm-workspace.yaml", "tests/security/state.test.ts", "docs/native-contract.md", "src/security/identity-actions.ts", "src/shell/IdentityActions.tsx", "src/security/identity-actions.test.ts", "app.json", "plugins/withIdentityProtection.cjs", "docs/development.md", "docs/architecture.md", "runtime/src/main.ts", "src/runtime/NappletHost.tsx", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt", "modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt", "runtime/tools/build.mjs", "runtime/fixtures/lock.json", "runtime/tests/runtime.spec.ts"]

autonomous: true

requirements: ["UX-01", "UX-02", "UX-09", "ID-01", "ID-02", "ID-03", "ID-04", "ID-05", "ID-06", "STATE-01", "FIX-01"]

estimate: {"tokens": 49000, "raw_tokens": 49000, "tasks": 3, "confidence": "low"}

must_haves: {"truths": ["First launch creates one securely stored identity and later launches reuse it; missing/corrupt established storage never creates another.", "Import and whole-shell switching preserve encrypted identities, isolate saved state and invalidate old sessions atomically.", "Explained export and separate deletion require fresh device authentication and never expose secrets after cancellation/background."], "artifacts": [{"path": "src/security/identity-vault.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/security/identity-transition.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/security/scoped-storage.ts", "provides": "Required executable implementation or direct acceptance evidence"}, {"path": "src/security/identity-actions.ts", "provides": "Required executable implementation or direct acceptance evidence"}], "key_links": [{"from": "src/shell/Settings.tsx", "to": "src/security/identity-transition.ts", "via": "single confirmed global identity switch"}, {"from": "src/security/identity-vault.ts", "to": "src/security/shell-database.ts", "via": "readback-verified protected key plus durable journal"}, {"from": "runtime/src/capabilities.ts", "to": "src/security/scoped-storage.ts", "via": "native-bound user/publisher/app namespace"}]}

---



<objective>
Secure identities, durable workspace and scoped data. Implements D-03, D-07, D-08, D-09, D-10.

Purpose: advance the complete independent Android proof without replacing it with the first trace.
Output: working slice, direct verification and updated source documentation.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-independent-android-proof/02-CONTEXT.md
@.planning/phases/02-independent-android-proof/02-RESEARCH.md
@docs/architecture.md
@docs/development.md
@docs/native-contract.md
</context>

<execution_notes>
Commands run from repository root after enabling pinned tools. New commands/scripts named below are created in their owning task before verification; they do not exist in the scaffold baseline. First failing check is expected until implementation. Native checks need a live explicitly selected target and installed current build; use the tracked driver to observe state and preserve process handles. Short unit checks run after each change; native build/device suites may exceed 60 seconds and must run with retained handles and progress updates. No source push or remote changes. Do not mark pending downstream requirements satisfied by a narrower trace. Some local-module/fixture packaging tasks exceed the five-file guideline because the requested six milestones retain coupled source/manifest/lock/test artifacts; execute as cohesive task commits and split execution internally if context requires, never omit files or behavior.
</execution_notes>

<tasks>

<task type="tracer" tdd="true">

<name>Task 1: Trace secure first-launch identity through header and restart</name>

<files>package.json, pnpm-lock.yaml, index.ts, src/security/crypto-bootstrap.ts, src/security/identity-vault.ts, src/security/shell-database.ts, src/security/identity-vault.test.ts, src/shell/Header.tsx, tests/native/driver.py</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, </read_first>

<behavior>Entropy unavailable fails; initialized-but-key-null fails; write/readback mismatch fails; crash staging recovers same staged key; ordinary and explicit-empty restart differ.</behavior>

<action>Per D-03/D-07/D-08 select exact compatible Expo/Applesauce/Nostr baseline from R-04; install native secure-random bootstrap before Nostr imports and fail on missing entropy. Implement SecureStore validated per-identity envelopes plus discoverable staging/receipt, SQLite nonsecret initialized receipt/inventory/journal/workspace, readback-confirmed writes and no secret in DB. Initialize only when successful reads prove no prior inventory; interrupted/missing/mismatched established identity is recovery UI. Provide real selected npub header/fallback and full npub copy in settings. Restore order/last-active/lazy other sessions/explicit empty from durable descriptors. Add initial unit failure cases and native same-key force-stop/relaunch trace; never reset identity between restart assertions.</action>

<verify><automated>node --experimental-strip-types --test src/security/identity-vault.test.ts && python3 tests/native/driver.py --serial emulator-5554 identity-restart --output .tools/evidence/02-03-init</automated></verify>

<done>Native first launch shows a real generated npub; forced restart reuses it and workspace; initialization/key-loss failure blocks safely without replacing identity.</done>

</task>

<task type="auto" tdd="true">

<name>Task 2: Implement import, atomic whole-shell switch and isolated State Lab</name>

<files>src/security/identity-vault.ts, src/security/identity-transition.ts, src/security/scoped-storage.ts, src/shell/Settings.tsx, runtime/src/capabilities.ts, napplets/state-lab/package.json, napplets/state-lab/src/main.ts, napplets/state-lab/vite.config.ts, napplets/state-lab/tsconfig.json, napplets/state-lab/index.html, napplets/state-lab/pnpm-lock.yaml, napplets/state-lab/pnpm-workspace.yaml, tests/security/state.test.ts, docs/native-contract.md, runtime/src/main.ts, src/runtime/NappletHost.tsx, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostModule.kt, modules/napplet-host/android/src/main/java/org/nostrocket/hypergolic/host/NappletHostView.kt, runtime/tools/build.mjs, runtime/fixtures/lock.json, runtime/tests/runtime.spec.ts</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/native-contract.md</read_first>

<behavior>Invalid/cancelled imports keep identity; old session requests fail after switch; cancelled switch retains live edits; namespace cross-product cannot read another value; staged imports recover without replacing selection.</behavior>

<action>Extend runtime/tools/build.mjs and the byte-verified bundled fixture manifest inventory to include State Lab, with a build-time allowlisted fixture map and native-owned selection before the native trace. Each fixture retains independently checked original bytes, hash, aggregate and declared capabilities. Add runtime tests proving only registered fixture identities select their expected bytes; merely building its napplet directory is insufficient. Extend the actual diagnostic-only integration seam before State Lab runs: runtime/src/main.ts, the Expo wrapper and native Module/View must carry narrow typed storage and read-only selected-public-identity request/reply adapters bound to the native generation and shell-owned user/publisher/stable-app context. Select the owning NAP storage and identity revisions and document its exact wire fields/errors before implementation. Kehto source binding remains mandatory; native independently resolves registered context and enforces bounds, quotas and revocation. Never trust caller-supplied scope or use WebView browser storage as the backend. Test wrong source/generation/namespace, async reply after close/switch and successful State Lab round trip through the actual native storage service. Per D-08/D-09 pin/review owning identity/storage NAP revisions and exact envelopes/errors before exposing domains. Support nsec-only trimmed import, scalar validation, readback/persistence, duplicate import, preserved saved identities and separate selector. One transition lock validates destination and persists selection before epoch increment; cancel/failure preserves all sessions, accepted switch pauses/revokes/destroys/recreates every view and its subscriptions under native-bound identity/publisher/stable-app/version/generation. SQLite storage quotas and prepared queries derive namespace only from that bound context; version not part of namespace. Build State Lab with reviewed authoring skills, same isolated fixture pipeline and real identity/storage calls, visible unsaved/saved/reset state. Include second app ID and publisher fixtures for isolation and same-version update continuity; no privileged test storage API. Document dirty reporting contract only if owning proposal actually exists; unknown remains protected.</action>

<verify><automated>node --experimental-strip-types --test tests/security/state.test.ts && pnpm --dir napplets/state-lab verify && python3 tests/native/driver.py --serial emulator-5554 identity-state --output .tools/evidence/02-03-state</automated></verify>

<done>Import/switch/restart across two identities and multiple sessions preserves encrypted identities and fully isolates native saved data across user/publisher/app while same-app versions retain access.</done>

</task>

<task type="auto" tdd="true">

<name>Task 3: Implement authenticated manual backup and explicit deletion</name>

<files>src/security/identity-actions.ts, src/shell/IdentityActions.tsx, src/security/identity-actions.test.ts, app.json, plugins/withIdentityProtection.cjs, tests/native/driver.py, docs/development.md, docs/architecture.md</files>

<read_first>AGENTS.md, docs/architecture.md, .planning/phases/02-independent-android-proof/02-CONTEXT.md, .planning/phases/02-independent-android-proof/02-RESEARCH.md, docs/development.md, docs/architecture.md</read_first>

<behavior>PIN-only works, no-lock fails closed, identity switch invalidates old auth, deleting journal survives crash, no plaintext secret appears in captured diagnostics.</behavior>

<action>Per D-10 and R-06, explain nsec ownership then require strong biometric or device credential; PIN-only is supported and auth-unavailable disables action. Bind a one-use short-lived auth result to exact action/identity/epoch/settings session. Reveal only in trusted capture-protected modal; no automatic clipboard/share, secret in accessibility/logs/persistence or app-switcher thumbnail; clear on dismiss/user background. Keep OS auth transient inactive separate from actual ownership loss. Deletion separately confirms/authenticates inactive identity only after another usable identity selected; journal deletion, verify absence, recover tombstone without restoring secret. Exclude key storage/cloud/device-transfer backup and inspect generated manifest. Unit test failed/late auth and all journal boundaries; native no-lock/PIN/biometric/cancel/background flows. Physical auth results are final 02-06 gate.</action>

<verify><automated>node --experimental-strip-types --test src/security/identity-actions.test.ts && pnpm run prebuild:android && python3 tests/native/driver.py --serial emulator-5554 identity-auth --output .tools/evidence/02-03-auth</automated></verify>

<done>No failed/cancelled/obsolete auth reveals/deletes; last identity cannot be deleted; backup reimports as same public identity; actual backup configuration and native protection are inspected.</done>

</task>

</tasks>

<verification>
Run every task check against current source. Record app/fixture hashes and direct native evidence separately from browser and unit results. Inspect git diff --check, status and remotes. Update must-have evidence; any missing dependent external input remains open.
</verification>

<success_criteria>
All plan truths are proven at their stated scope; task checks pass or an existing explicitly documented toolchain mismatch is identified without masking new failures. No new silent-signing/native authority or unrelated scope. Full phase completion remains subject to final requirement-by-requirement audit in 02-06.
</success_criteria>

<output>
Create .planning/phases/02-independent-android-proof/02-03-SUMMARY.md, recording changes, exact checks, current artifact/device evidence and unresolved rows.
</output>
